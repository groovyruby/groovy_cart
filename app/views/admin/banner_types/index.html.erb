<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", admin_banner_types_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_banner_type_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> BannerTypes</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.admin_banner_type.name", :default => t("activerecord.labels.name", :default => "Name")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @banner_types.each do |banner_type| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= banner_type.id %>
          </td>
                    <td>
            <%= link_to banner_type.name, admin_banner_type_path(banner_type) %>
          </td>
                    <td>
            <%= banner_type.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", admin_banner_type_path(banner_type) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_admin_banner_type_path(banner_type) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", admin_banner_type_path(banner_type), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
